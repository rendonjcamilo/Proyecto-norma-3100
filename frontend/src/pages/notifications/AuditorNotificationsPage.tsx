/**
 * Auditor Notifications Page
 * Page wrapper for auditor notification sender
 */

import React from 'react';
import { AuditorNotificationSender } from '../../components/Notifications/AuditorNotificationSender';

export const AuditorNotificationsPage: React.FC = () => {
  return <AuditorNotificationSender />;
};
